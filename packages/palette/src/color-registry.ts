import * as base from "./index";

export const colorRegistry = {
    primary: {
        red: `rgb(${base.palette.red[500]})`,
        green: base.palette.green[500],
        blue: base.palette.blue[500],
    },
};